
declare interface DEFAULT_SEARCH_FILTERSType {
	static order: string;

	static duration: string;

	static published: string;
}

declare interface paramsType {
	useEffect(fetchVideos: any, DEFAULT_SEARCH: any, DEFAULT_SEARCH_FILTERS: any, fetchVideos: any, useEffect: any, if: any, visibleVideos: any, length: any): null;
}
